extends layout

block content
  .content
    .categories-wrapper
      h1 #{title}
      div.category-container
        each category in categories
          .category
            h3
              a(href=category.url) #{category.name}
            div #{category.formatDescription}
            .read-more
              hr
              a(href=category.url) Read more

      